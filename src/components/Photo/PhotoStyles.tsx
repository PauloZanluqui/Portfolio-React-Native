import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderRadius: 500,
    width: 100,
    height: 100,
    borderWidth: 2,
    borderColor: 'black'
  }
});